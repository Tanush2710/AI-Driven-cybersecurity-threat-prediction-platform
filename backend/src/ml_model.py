import random

class ThreatModel:
    def __init__(self):
        # Placeholder for model initialization
        pass

    def predict(self, features):
        """
        Predicts threat level based on features.
        
        Args:
            features (dict): A dictionary of features extracted from traffic data.
            
        Returns:
            pred (int): 1 for normal, -1 for anomaly.
            score (float): Anomaly score (0.0 to 1.0).
        """
        
        pred = 1 
        score = 0.0
        
        return pred, score
